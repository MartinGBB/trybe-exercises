use('storage');
db.products.aggregate([
  {
    $project: {
      _id: 0,
      name: 1,
      lucro: {
        $abs: {
          $subtract: ["$sale_price",
          { $add: ["$taxes", "$purchase_price"] }
          ]
        }
      }
    }
  }
]);
